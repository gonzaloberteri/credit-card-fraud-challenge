subscription GetActionLogs {
  action_logs(order_by: { date: desc }, limit: 60) {
    id
    type
    date
    provider
    extra_data
  }
}

query GetActionLogsOnce {
  action_logs(order_by: { date: desc }, limit: 60) {
    id
    type
    date
    provider
    extra_data
  }
}